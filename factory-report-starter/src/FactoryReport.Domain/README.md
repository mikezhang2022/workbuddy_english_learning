# FactoryReport.Domain

领域模型层。不依赖 Infrastructure、EF Core、HTTP、UI 或任何数据库驱动。

本阶段（阶段 3）提供数据库无关的第一版领域对象与值对象，供后续 Fake 数据、Oracle 映射与报表引擎使用。

**本阶段不包含 Oracle 表结构、EF Migration、DDL 或 Schema 脚本。**

详见 `docs/domain-model.md`。
